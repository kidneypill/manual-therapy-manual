{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json; d=json.load(sys.stdin); fp=d.get('tool_input',{}).get('file_path','') or d.get('tool_response',{}).get('filePath',''); exit(0 if ('Concepts' in fp and fp.endswith('.md')) else 1)\" && cd \"Z:/Projects/Manual-Therapy-Manual\" && python sync_concepts.py 2>&1 || true",
            "statusMessage": "Concepts 변경 감지 — sync_concepts.py 실행 중..."
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json; d=json.load(sys.stdin); cmd=d.get('tool_input',{}).get('command',''); sys.exit(0) if 'check_integrity' not in cmd else sys.exit(1)\" || cd \"Z:/Projects/Manual-Therapy-Manual\" && python automators/fix_integrity.py --dry-run 2>&1",
            "statusMessage": "check_integrity 완료 — fix_integrity.py dry-run 실행 중..."
          }
        ]
      }
    ]
  }
}
